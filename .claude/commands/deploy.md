Deploy the portfolio site to GitHub Pages.

Steps:
1. Run `npm run build` (or `astro build` / `next build`) and verify no errors
2. Check that a CNAME file exists in the output/dist folder with the custom domain
3. Commit all changes with message "deploy: update portfolio site"
4. Push to main branch
5. Report the live URL and any issues

If there are build errors, fix them before deploying.
